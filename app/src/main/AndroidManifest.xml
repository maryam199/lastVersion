<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appterm.mydietician">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyDietician">
        <activity android:name=".ChangeFoodActivity"></activity>
        <activity android:name=".ChatActivity" />
        <activity android:name=".AllDietitiansActivity" />
        <activity
            android:name=".HomeDietitianActivity"
            android:label="@string/title_activity_home_dietician"
            android:theme="@style/Theme.MyDietician.NoActionBar" />
        <activity android:name=".UploadCertificationActivity" />
        <activity android:name=".DisplayRecipeActivity" />
        <activity
            android:name=".HomeClientActivity"
            android:label="@string/title_activity_home_client"
            android:theme="@style/Theme.MyDietician.NoActionBar" />
        <activity android:name=".ClientQuizFiveActivity" />
        <activity android:name=".ScannerActivity" />
        <activity android:name=".ClientQuizFourActivity" />
        <activity android:name=".ClientQuizThreeActivity" />
        <activity android:name=".SelectRegisterTypeActivity" />
        <activity android:name=".ClientQuizTwoActivity" />
        <activity android:name=".ClientQuizOneActivity"></activity>
        <activity android:name=".ClientInformationActivity" />
        <activity android:name=".RegisterAndLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.CAMERA" />

</manifest>